{
  "version": 2,
  "buildCommand": "cd landing && npm install && npm run build",
  "outputDirectory": "landing/dist",
  "rewrites": [
    {
      "source": "/(login|dashboard|gerente|exportadora|agricultor)(.*)",
      "destination": "/index.html"
    }
  ]
}
